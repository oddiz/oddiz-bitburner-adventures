import { NS } from "/typings/NetscriptDefinitions";

export class SingularityManager {
    private ns: NS
    constructor(ns: NS) {
        this.ns = ns;
    }

    run = async () => {
        this.ns.singularity.
        }
}